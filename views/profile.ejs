<%- include('header') %>

<% if (user.identity === 'teacher') { %>
<h1 class="ui segment"><%= user.name %>发布的课程</h1>

<% } else { %>
<h1 class="ui segment"><%= user.name %>参与的课程</h1>
<% } %>

<% posts.forEach(function (post) { %>
<%- include('components/post-content', { post: post }) %>
<% }) %>

<%- include('footer') %>